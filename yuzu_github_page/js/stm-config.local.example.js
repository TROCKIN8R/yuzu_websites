/**
 * Optional local overrides — copy to stm-config.local.js (gitignored).
 *
 * Use this only for non-secret settings, e.g. pointing at a local Edge Function:
 *
 * window.STM_CONFIG_LOCAL = {
 *   supabase: {
 *     url: "http://127.0.0.1:54321",
 *     functionName: "stm-proxy"
 *   }
 * };
 *
 * Do NOT put your STM API key in this file or any client-side JavaScript.
 */
